# 当前开发焦点

## 状态

**当前为空。** 2026-08-10 获准的 Hermes M1/M2 已完成：Responses
`reasoning.encrypted_content` include 按精确候选接受，`parallel_tool_calls:true` 按精确候选开放。
确认实现事实、上游对照结果、失败优先测试和验证边界已转入 `docs/implementation-status/`。

Chat `stream_options.include_usage` 与 `json_schema` 仍是已确认但未获准修改的兼容边界；开始其中任一行为前，必须重新建立单一、
获准的短周期焦点，写明完整候选证据、失败测试、非目标与验证范围。
